import { DobValidatorDirective } from './dob-validator.directive';

describe('DobValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DobValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
